---
name: child-ai-dialogue-analysis
description: 儿童与 AI 语音助手对话的心理与成长分析。从 AI 助手控制台（如小智 xiaozhi.me）提取孩子与 AI 的聊天记录，量化语言发展、情绪状态、兴趣图谱、社交认知，并生成结构化的儿童成长观察报告。触发词：孩子和AI对话分析、儿童心理分析、成长状态分析、AI对话记录分析、孩子聊天记录分析、xiaozhi分析。
---

# Child AI Dialogue Analysis

分析儿童与 AI 语音助手（如小智 / xiaozhi.me「随身」智能体）的对话记录，评估孩子的**语言发展、心理状态、兴趣主题、社交认知与成长里程碑**，输出给家长一份可读、客观、有建设性的观察报告。

> ⚠️ 本 skill 用于**家长分析自己孩子的数据**。请确保你拥有所分析对话数据的合法访问权限，且报告客观、建设性、保护儿童隐私。

## 何时使用

用户要求：
- 分析孩子和 AI 助手/语音助手的聊天记录
- 了解孩子的心理状态、成长状态、语言发展
- 提取 AI 控制台里的对话记录做分析
- 判断孩子使用 AI 是否过度 / 是否有需要关注的信号

## 核心原则

1. **数据优先**：先拿到真实对话数据，再谈分析。不要凭空猜测。
2. **发展心理学视角**：孩子很多"问题"（脏话、战斗幻想、死亡游戏）都是正常发展阶段，先判断"是否符合年龄段特征"，再谈"是否需要关注"。
3. **客观建设性**：报告要区分「正常发展」「亮点」「需要留意」，给家长可执行建议，不贴标签、不吓唬。
4. **隐私保护**：报告可用化名/首字母，不暴露真实身份、学校、住址、手机号等敏感信息。
5. **区分幻想与现实**：孩子角色扮演、编故事、说"打死怪兽"等，多数是象征性的，不代表攻击倾向。要结合上下文判断。

## 工作流

### 1. 获取数据

有两种来源路径：

**路径 A：浏览器控制台抓取（推荐，如 xiaozhi.me）**

1. 用浏览器打开 AI 助手控制台（如 `https://xiaozhi.me/console/agents`），确认已登录。
2. 定位孩子实际使用的智能体（看活跃度、角色配置、设备绑定）。
3. 通过浏览器 DevTools hook 找真实 API（如 `/api/agents/{agent_id}/chats`、`/api/chats/messages?chatId={id}`）。
4. 在页面上下文用 `fetch` 批量拉取所有对话与消息，存到 `window.__allChats`。
5. 用 CDP（`scripts/export_cdp.js`）导出到 `raw/all_messages.json`。
6. 注意：控制台接口可能有分页限制（如只返回最新 50 个对话），需核实数据完整性。

**路径 B：已有数据文件**

如果用户已提供 `all_messages.json`（格式见 `references/data_format.md`）或原始导出文件，直接进入分析。

### 2. 数据清洗与基础统计

```bash
python3 scripts/analyze.py              # 总览：对话数/消息数/时间范围/角色分布/按天统计
python3 scripts/extract_user_msgs.py    # 提取孩子发言 → user_messages.txt
python3 scripts/extract_transcript.py   # 生成带 AI 回复摘要的完整对话 → transcript.txt
```

- `analyze.py` 输出：对话数、消息总数、时间范围、用户/AI/工具消息数、按天统计、对话标题列表。
- **先人工通读孩子发言**（`user_messages.txt`），这是分析质量的关键。不要只看词频。
- 注意语音转写（ASR）误差：孩子的话可能被转错（如"西红柿"→"西红吃"），理解时结合上下文。

### 3. 量化分析

```bash
python3 scripts/theme_stats.py      # 主题词频 + 表情统计 + 消息长度分布
python3 scripts/behavior_stats.py   # 规则意识/社交/情绪/照顾/死亡游戏等行为模式
python3 scripts/time_analysis.py    # 每天时长/时段分布/平均语长趋势/负面情绪样本
```

- 主题关键词集中在 `config/themes.json`，可按孩子/文化背景调整。
- 量化结果只作"线索"，最终判断要回到原文。

### 4. 人工阅读与定性分析

- 通读 `user_messages.txt`，重点关注：
  - **情绪词上下文**：生气/害怕/难过的具体场景（谁惹的？为什么？）
  - **依恋对象**：孩子反复提到的角色/朋友/亲人（幻想伙伴往往是情感需求的投射）
  - **真实生活线索**：提到学校、朋友名字、家人、健康约束（如"不能吃糖"）、受伤等
  - **规则与公平**：孩子是否自己制定规则、当裁判、计较输赢
  - **语言复杂度**：是否讲长故事、用复句、主动提问、正确数数
  - **幻想与现实边界**：孩子是否清楚"动画片是假的""我们俩在玩"

### 5. 生成报告

按 `templates/report_template.md` 组织，参考 `references/child_psychology.md` 的评估框架：

- 整体概况（数据量、时间线、一句话总结）
- 语言发展评估（优势 + 关注点）
- 心理发展评估（想象/规则/情绪/依恋/社交/游戏化暴力）
- 兴趣图谱
- 成长里程碑判断
- 给家长的建议（放心 / 引导 / 留意）

建议用 `scripts/report_generator.py` 自动填充统计部分，再人工补充定性观察。

### 6. 交付

- 报告可直接在会话中回复，也可通过飞书/邮件等渠道发送给家长。
- 如有需要，可导出为 Markdown / PDF / 飞书文档。
- 归档：把报告存到 `output/`，把关键观察追加到 `state/notes.md`（便于多期对比）。

## 报告结构（模板摘要）

1. 数据来源与范围
2. 整体概况（含数据速览卡）
3. 语言发展评估
4. 心理发展评估
5. 兴趣图谱
6. 成长里程碑判断
7. 给家长的建议（✅放心 / 🌱引导 / ⚠️留意）

## 重要解读原则

### 儿童发展阶段常识（勿过度解读）

| 表现 | 发展阶段 | 建议 |
|---|---|---|
| 说"屎尿屁"脏话 | 3-6岁"污言秽语期" | 正常，不干预 |
| 战斗幻想、"打死怪兽" | 象征性游戏 | 正常，区分幻想与现实即可 |
| 计较输赢、自己定规则 | 前运算期→具体运算期过渡 | 引导棋类等规则游戏 |
| 幻想伙伴（如"美琪"） | 情感联结与想象发展 | 关注背后的陪伴需求 |
| 同音错字（ASR/发音） | 语音发展后期 | 6岁后仍明显可咨询 |
| 生气后快速恢复 | 情绪弹性好 | 正向 |

### 需要真正留意的信号（需结合上下文）
- 持续严重的负面情绪（哭闹、退缩、攻击真人）
- 反复表达害怕某物/某人且影响生活
- 幻想与现实长期混淆（分不清动画和现实）
- 没有真实同伴、完全依赖 AI 社交
- 单日使用时间过长（建议 ≤1-2 小时，且大人陪伴）

## 注意事项

- **数据权限**：只分析你拥有合法访问权的数据（通常是自己的孩子/家人的设备）。
- **隐私**：报告避免出现真实姓名、手机号、地址、学校全名。
- **不诊断**：本 skill 是"家长观察工具"，不提供医学/心理学诊断。如有担忧建议咨询专业人士。
- **ASR 误差**：语音转写可能有错字，判断语言能力时结合上下文，别因转写错误误判。
- **AI 的迁就**：AI 助手通常无限迁就孩子、顺着他的规则，长期可能让孩子对真实社交（朋友会说"不"）缺乏准备。建议家长增加真实同伴玩耍。
- 报告的最终价值在于**家长陪伴与理解**，不是贴标签或评判。

## 目录

```
child-ai-dialogue-analysis/
├── SKILL.md                 # 本文件
├── README.md                # 开源说明
├── LICENSE                  # MIT
├── config/
│   └── themes.json          # 主题关键词配置（可自定义）
├── references/
│   ├── child_psychology.md  # 儿童发展心理学评估框架
│   ├── data_format.md       # all_messages.json 数据格式说明
│   └── interpretation.md    # 解读原则与警示信号
├── scripts/
│   ├── analyze.py           # 基础统计
│   ├── extract_user_msgs.py # 提取孩子发言
│   ├── extract_transcript.py# 生成完整对话文本
│   ├── theme_stats.py       # 主题/表情/长度统计
│   ├── behavior_stats.py    # 行为模式统计
│   ├── time_analysis.py     # 时间/情绪趋势
│   ├── report_generator.py  # 自动生成报告框架
│   └── export_cdp.js        # CDP 导出浏览器数据
├── templates/
│   └── report_template.md   # 报告模板
└── examples/
    └── sample_report.md     # 示例报告（脱敏）
```
